name: Build and Test BML WPF App

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up .NET
      uses: actions/setup-dotnet@v2
      with:
        dotnet-version: 6.0.413

    - name: Restore dependencies
      run: msbuild BML/BML.csproj /t:Restore /p:Configuration=Debug

    - name: Build and test
      run: |
        msbuild BML/BML.csproj /p:Configuration=Debug
        dotnet test BML.Tests/BML.Tests.csproj
